Export your three workflows from the n8n UI (workflow -> ... menu ->
Download) and place them here as WF-1-profile-intake.json,
WF-2-conversation-capture.json, WF-3-feedback.json. They contain no
secrets: all keys are referenced via $env and live in n8n/.env.
